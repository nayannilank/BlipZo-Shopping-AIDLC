{
  "framework": "vite",
  "buildCommand": "cd ../.. && npm install && npx turbo build --filter=@blipzo/web --force",
  "outputDirectory": "dist",
  "installCommand": "echo 'skipped'",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
